
1. 3 sample csv files with names appended with dates.
2. Running the python script with the path given inside the script will fetch the files and create DDLs

The sql file from the csvkit tool will generate the csv and ouput it to test.sql

the final output sql for executing in snowflake is final_output.sql
